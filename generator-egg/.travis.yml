language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-egg' ]; then cd .. && eval "mv $currentfolder generator-egg" && cd generator-egg; fi

