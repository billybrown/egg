'use strict';

// Empties folders to start fresh
module.exports = {
  options: {
    alwaysInstall: false,
    errorLevel: 'fatal',
    globals: [],
    maxBuffer: 200*1024,
    nvm: true,
    override: ''
  }
};
